# /etc/skel/.bashrc
#
# This file is sourced by all *interactive* bash shells on startup,
# including some apparently interactive shells such as scp and rcp
# that can't tolerate any output.  So make sure this doesn't display
# anything or bad things will happen !


# Test for an interactive shell.  There is no need to set anything
# past this point for scp and rcp, and it's important to refrain from
# outputting anything in those cases.
if [[ $- != *i* ]] ; then
	# Shell is non-interactive.  Be done now!
	return
fi

PS1='\[\033[01;33m\]\u \[\033[01;34m\]@\[\033[01;34m\]\h\[\033[01;36m\] \w \$\[\033[00m\] '

HISTFILESIZE='2000'

[[ -f /etc/bash_completion ]] && source /etc/bash_completion

# Put your fun stuff here.
#alias ssh='ssh -Y'
#alias tree='tree -A'
alias toff='synclient TouchpadOff=1'
alias ton='synclient TouchpadOff=0'
alias vi='vi -p'
alias bi='vi'
alias ls='ls --color=auto --group-directories-first'
alias grep='grep --color=auto -E'
alias diff='diff -urN'
alias ll='ls -l'
alias la='ls -a'
alias lh='ls -d .??*'
#alias dmesg='dmesg -H'
alias astyle='astyle -Y'
alias bc='bc -q'
alias git='LANG=en_US.UTF-8 git'

PATH="~/bin:/usr/local/sbin:/usr/sbin:/sbin:/media/arquivos/bin:/usr/lib/oracle/product/11.2.0/xe/bin:${PATH}"
#XSESSION="fluxbox"
PYTHONSTARTUP=$HOME/.pythonrc
export PYTHONSTARTUP

# turn off bell
[[ -n ${DISPLAY} && ${DISPLAY} != "localhost:10.0" ]] && xset b off

export EDITOR="vim"
export FIGNORE="svn"
export WORKSPACE="/mnt/files"

#ssh -Cl machado -L 2222:taubenus:22 allesodernix.zlw-ima.rwth-aachen.de
#ssh machado@visacardus -L 8000:chronos:80 
